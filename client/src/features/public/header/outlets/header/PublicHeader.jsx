import React from "react";
import { useSelector } from "react-redux";
import MobileHeader from "../../components/layout/mobileViewHeader/MobileHeader";

const PublicHeader = () => {
  return <MobileHeader />;
};

export default PublicHeader;
